export enum ERROR_LEVEL_ENUM {
    FATAL = 1,
    ERROR,
    WARN,
    INFO,
    DEBUG
}

export enum EVENT_TYPE_ENUM {
    CONSOLE,
    CRASH,
    XHR,
    FETCH,
    NETWORK,
    LIFECYCLE,
    DOM,
    PROMISE,
    SCRIPT,
    RESOURCE,
    UNKNOWN,
    CUSTOM
}